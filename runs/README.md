# Runtime Run Logs

This directory is for local audit logs, manifests, and run ledgers. Runtime
payloads are ignored by git and should not be committed from this path.

Use `tests/fixtures/`, `docs/archive/`, or `artifacts/paper/` for material that
must become source-controlled evidence.
