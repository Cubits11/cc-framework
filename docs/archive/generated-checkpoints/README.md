# Archived Generated Checkpoints

This directory preserves historical checkpoint examples that used to be tracked
under the runtime-only `checkpoints/` root.

These files are archived evidence, not live runtime output and not required
release artifacts. New checkpoint outputs should stay under `checkpoints/` and
remain ignored unless a maintainer explicitly promotes them with documentation.
