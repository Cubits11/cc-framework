# Archived Two-World Game Result

This directory preserves the historical root-level `game.csv` output from the
lightweight two-world console game.

The live game writes CSV logs only when called with `--log`. New logs should be
written under an ignored runtime path such as `results/` or `runs/` unless a
maintainer deliberately promotes them as a fixture, archive, or release
artifact.
