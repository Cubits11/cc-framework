# Archived Smoke Generated Results

This directory preserves historical smoke-run output that used to be tracked
under `results/smoke/`.

The live smoke reproduction path still writes to `results/smoke/`, but that path
is now runtime-only and ignored by git.
