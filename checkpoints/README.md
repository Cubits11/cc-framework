# Runtime Checkpoints

This directory is for local experiment checkpoints only. Checkpoint payloads are
ignored by git and should not be committed from this path.

Historical checkpoint examples that were once tracked in this repository now
live under `docs/archive/generated-checkpoints/`.
