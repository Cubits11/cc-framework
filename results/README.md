# Runtime Results

This directory is for local experiment results only. Generated result payloads
are ignored by git and should not be committed from this path.

Result files that are promoted into tests, documentation, or release evidence
must move into `tests/fixtures/`, `docs/archive/`, or `artifacts/paper/`.
